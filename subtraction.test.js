const subtraction = require('./subtraction');
test('subtracts 4 - 2 to equal 2', () => {
   expect(subtraction(4, 2)).toBe(2);
});

const add = require('./subtraction');
test('add 4 + 3 to equal 6', () => {
    expect(add(4,2)).toBe(6);
})